import { createElement } from "../../shared/createElement.js";

export function editModeButtonsElement () {
    const element = createElement('button', ['editMode_dtn'], {}, 'add');

    return element;
};