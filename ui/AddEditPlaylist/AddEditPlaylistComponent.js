import { createElement } from "../../shared/createElement.js";

export function AddEditPlaylistComponent () {
    const element = createElement('dialog', ['dialog'], {open});
    element.innerHTML = `<p>asdf</p>`
    return element;
};